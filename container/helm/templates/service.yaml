apiVersion: v1
kind: Service
metadata:
  name: {{ include "elan-standard-web.fullname" . }}
  labels:
    {{- include "elan-standard-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elan-standard-web.selectorLabels" . | nindent 4 }}
